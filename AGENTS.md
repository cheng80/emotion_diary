# 프로젝트 작업 규칙

- 이 프로젝트에서는 기능 구현·수정 시 Worktree나 feature 브랜치를 만들지 않는다.
- 현재 checkout의 `main` 브랜치에서 직접 작업한다.
- 검증을 완료한 변경은 `main` 브랜치에 직접 커밋할 수 있다.
- 원격 push, Pull Request 생성·병합은 사용자가 명시적으로 요청한 경우에만 수행한다.
