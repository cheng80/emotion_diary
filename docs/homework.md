# 날짜별 과제 기능 구현 목록

## 표시 기준

- [x] **정상 구현**: 과제 요구사항과 같은 시점과 방식으로 동작
- [x] **대체 구현**: 기능은 구현되어 있지만 시점이나 방식이 과제 요구사항과 다름
- [ ] **미구현**: 현재 코드에 해당 동작이 없음

## 08/05 과제

### CSS: 일기 목록

- [x] **정상 구현** — 스크롤을 내려도 필터가 헤더 아래에 고정됨

### CSS: 일기 삭제

- [x] **정상 구현** — 카드 이미지 위에 삭제 아이콘을 겹쳐 배치함

### JavaScript: 일기 목록

- [x] **정상 구현** — 플로팅 버튼을 선택하면 화면 맨 위로 부드럽게 이동함

### JavaScript: 일기 상세

- [x] **정상 구현** — 회고를 입력하고 일기에 저장함
- [x] **대체 구현** — 상세 화면 진입 시가 아니라 회고 등록 후 회고 목록 위치로 부드럽게 이동함

### JavaScript: 일기 삭제

- [x] **정상 구현** — 목록 카드에서 일기를 삭제함
- [x] **정상 구현** — 삭제 버튼 이벤트가 카드 클릭으로 전달되지 않도록 막음

## 08/06 과제

### CSS: 전체

- [x] **정상 구현** — 반응형 디자인을 적용함
- [x] **정상 구현** — 브레이크포인트에서 모바일 화면으로 전환함
- [x] **정상 구현** — 주요 크기 값을 `rem` 단위로 작성함

### CSS: 일기 목록

- [x] **정상 구현** — 모바일에서 카드 목록을 grid로 전환함
- [x] **정상 구현** — 모바일 한 줄에 카드 두 개를 배치함

### JavaScript: 일기 목록

- [x] **정상 구현** — 스크롤 위치와 관계없이 플로팅 버튼을 화면 오른쪽 아래에 고정함

## 08/07 과제

### CSS: 일기 등록

- [x] **정상 구현** — 일기 등록 화면을 모달로 표시함
- [x] **정상 구현** — 등록 완료 시 `등록이 완료되었습니다.` 안내 중첩 모달을 표시함

### CSS: 일기 상세

- [x] **정상 구현** — 일기 내용을 복사하면 화면 하단에 토스트 애니메이션을 표시함

### JavaScript: 일기 등록

- [x] **대체 구현** — 등록 모달의 바깥 영역을 선택하면 취소 확인 모달을 표시함
- [x] **대체 구현** — 등록 모달에서 ESC 키를 입력하면 취소 확인 모달을 표시함

### JavaScript: 일기 상세

- [x] **정상 구현** — 내용 복사 버튼을 선택하면 일기 내용을 클립보드에 복사함
- [x] **정상 구현** — 삭제 버튼을 선택하면 삭제 확인 모달을 표시함

### JavaScript: 모달

- [x] **정상 구현** — 모달을 열 때 화면을 맨 위로 이동함
- [x] **정상 구현** — 모달을 연 동안 뒷배경 스크롤을 막음

## 08/10 과제

### CSS: 일기 목록

- [x] **정상 구현** — 일기 사진에 `object-fit: cover`를 적용해 여백 없이 표시함

### CSS: 사진 목록

- [x] **정상 구현** — 강아지 사진에 `object-fit: cover`를 적용해 여백 없이 표시
- [x] **정상 구현** — 선택한 비율에 따라 강아지 사진 목록을 전환
  - [x] **정상 구현** — 권장 기본형 `aspect-ratio: 1 / 1`
  - [x] **정상 구현** — 권장 가로형 `aspect-ratio: 4 / 3`
  - [x] **정상 구현** — 권장 세로형 `aspect-ratio: 3 / 4`
- [x] **정상 구현** — 강아지 사진 로딩 중 그라데이션 스켈레톤 이미지 표시

### JavaScript: 사진 목록

- [x] **정상 구현** — `async/await`와 `try...catch`로 강아지 이미지를 최대 10개 불러오고 새로 불러오기와 실패 시 재시도 제공
- [x] **정상 구현** — 요청 처리 중 `새로 불러오기` 버튼을 비활성화해 연속 클릭으로 인한 중복 요청 방지 (`photoLoading` 상태 재사용)
- [x] **정상 구현** — 일기보관함과 사진보관함을 JavaScript 컴포넌트로 구성하고 탭으로 전환

## 과제 이미지 외 추가 구현

- [x] 감정별 일기 필터
- [x] 제목과 내용의 검색어 포함 검색
- [x] 감정 필터와 검색 조건 동시 적용
- [x] 일기 등록 후 목록 즉시 갱신
