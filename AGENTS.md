# 프로젝트 작업 규칙

- 이 프로젝트에서는 기능 구현·수정 시 Worktree나 feature 브랜치를 만들지 않는다.
- 현재 checkout의 `main` 브랜치에서 직접 작업한다.
- 검증을 완료한 변경은 `main` 브랜치에 직접 커밋할 수 있다.
- 원격 push, Pull Request 생성·병합은 사용자가 명시적으로 요청한 경우에만 수행한다.

## 기술·문법 기준

- HTML, CSS, JavaScript 구현 시 `/Users/cheng80/Desktop/core_master_codes`의 기술과 문법 수준만 참고한다.
- 실제 구현 대상은 `homework/`이며, 완성본 참고자료는 `web/`이다.
- 과제 이미지와 `web/`의 구현이 다르면 완성본인 `web/`을 우선한다.
- 스크롤 감지 시 필터 배경색 반전은 제외된 실험 과제이므로 구현하거나 미완료 항목으로 기록하지 않는다.
